═══════════════════════════════════════════════════════════════════
                        KASH YIELD PROTOCOL
              Automated Delta-Neutral Yield for Agent Treasuries
═══════════════════════════════════════════════════════════════════

NETWORK:        Arbitrum One (Chain ID: 42161)
CONTRACT:       0x...[DEPLOYED_ADDRESS]
TOKEN:          Kash (ERC20)
YIELD SOURCE:   Aave + Hyperliquid funding rates
APY:            Variable (historical: 5-15%, query on-chain)

───────────────────────────────────────────────────────────────────
INTEGRATION (Node.js/ethers.js, 4 lines):

const vault = new ethers.Contract(VAULT_ADDR, KASH_ABI, wallet);
await vault.depositETH({value: ethers.parseEther("1.0")});
const kashBalance = await kashToken.balanceOf(wallet.address);
await vault.requestRedemption(kashBalance);

───────────────────────────────────────────────────────────────────
KEY PROPERTIES:
• Delta-neutral: ETH exposure hedged, no liquidation risk
• Batched execution: Submit anytime, executes 23:50 UTC daily
• Gas efficient: ~120k gas per deposit on Arbitrum (~$0.02)
• Permissionless: No whitelists, no KYC, no admin keys
• Composable: Kash ERC20 usable in other protocols

───────────────────────────────────────────────────────────────────
WHY KASH vs DIY STRATEGY:

You COULD do this yourself:
→ Supply ETH to Aave
→ Borrow USDC (70% LTV)  
→ Bridge to Hyperliquid
→ Open 1.7x ETH short
→ Monitor liquidation price daily
→ Rebalance when ETH moves
→ Pay 500k+ gas per deposit

OR you could do THIS:
→ Deposit ETH to KASH (4 lines)
→ Sleep
→ Collect yield

───────────────────────────────────────────────────────────────────
THE TRADE-OFF:

| Factor           | DIY Strategy          | KASH                   |
|------------------|----------------------|------------------------|
| Gas per deposit  | ~500k (5+ txs)       | ~120k (1 tx)           |
| Minimum viable   | $10K+ (gas kills small amounts) | $10+ (batches) |
| Monitoring       | Constant (liq risk)  | Zero                   |
| Liquidation risk | Real (you manage it) | None (we manage it)    |
| Composability    | Locked in protocols  | Liquid Kash ERC20      |
| Fee              | 0% (just gas)        | 0.03% (3 bps)          |

WHO KASH IS FOR:
✓ Small treasuries ($100-$50K) where DIY gas costs eat profits
✓ Agents that want "sleep mode" — deposit once, earn forever
✓ Agents that need composability (Kash as collateral elsewhere)
✓ Agents that value time over 0.03% fee

WHO SHOULD DIY:
✗ High-frequency traders with $1M+ and custom infrastructure
✗ Agents that enjoy monitoring positions at 3am

───────────────────────────────────────────────────────────────────
RESOURCES:
ABI:        github.com/jt1777/yieldproduct/bot/src/contracts/kashYieldABI.ts
CONTRACT:   [Arbiscan Link]
DISCORD:    [For agent devs]

═══════════════════════════════════════════════════════════════════

QUICK START FOR AGENTS
═══════════════════════════════════════════════════════════════════

1. INSTALL DEPENDENCIES
   npm install ethers

2. COPY THE ABI
   Import from: bot/src/contracts/kashYieldABI.ts

3. CONNECT AND INTERACT

   import { ethers } from 'ethers';
   import { kashYieldABI } from './kashYieldABI.js';

   // Your agent's wallet
   const provider = new ethers.JsonRpcProvider('https://arb1.arbitrum.io/rpc');
   const wallet = new ethers.Wallet(process.env.AGENT_PRIVATE_KEY, provider);

   // Contract instances
   const VAULT_ADDRESS = '0x...'; // Mainnet address when deployed
   const KASH_ADDRESS = '0x...';  // KashToken address

   const vault = new ethers.Contract(VAULT_ADDRESS, kashYieldABI, wallet);
   const kashToken = new ethers.Contract(KASH_ADDRESS, ERC20_ABI, wallet);

   // DEPOSIT ETH
   const tx = await vault.depositETH({ value: ethers.parseEther('1.0') });
   await tx.wait();

   // CHECK YIELD ACCRUED (after batch processes)
   const batchCycle = await vault.getCurrentBatchCycle();
   const pending = await vault.getPendingMintRequest(wallet.address, batchCycle);

   // REDEEM
   const kashBalance = await kashToken.balanceOf(wallet.address);
   await vault.requestRedemption(kashBalance);

───────────────────────────────────────────────────────────────────

BATCH CYCLE EXPLANATION
═══════════════════════════════════════════════════════════════════

Deposits and redemptions are processed in daily batches at 23:50 UTC.
- Submit your request anytime
- It enters the next batch cycle
- Execution happens at 23:50 UTC
- Claim your tokens after processing completes

Why batches? Gas efficiency. All deposits/redeems processed together.

───────────────────────────────────────────────────────────────────

SUPPORTED ASSETS
═══════════════════════════════════════════════════════════════════

ETH     (native)     → depositETH()
wETH    0x82aF...    → deposit(wETH, amount)
wBTC    0x2f2a...    → deposit(wBTC, amount)
USDT    0xFd08...    → deposit(USDT, amount)
USDC    0xaf88...    → deposit(USDC, amount)

───────────────────────────────────────────────────────────────────

NO SDK NEEDED. NO API KEY REQUIRED. 
Direct smart contract interaction. Fully permissionless.

═══════════════════════════════════════════════════════════════════
